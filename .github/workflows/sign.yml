name: Sign Module

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  sign:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v3

    - name: Import Code Signing Certificate
      run: |
        $cert = Import-PfxCertificate -FilePath certs/DriveTools.pfx -Password (ConvertTo-SecureString "${{ secrets.CERT_PASSWORD }}" -AsPlainText -Force) -CertStoreLocation Cert:\CurrentUser\My

    - name: Sign Module
      run: |
        Set-AuthenticodeSignature -FilePath src/DriveTools.psm1 -Certificate $cert
